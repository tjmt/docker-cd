version: '3.5'

services:
  app:
    image: ${DOCKER_REGISTRY}/app:${BRANCH}.${VERSION}
    environment:
      RUN_TEST: ${RUN_TEST:-false}
      ASPNETCORE_ENVIRONMENT: Production
    ports:
      - "80:80"       
      - "443:443"      
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '0.5'
          memory: 1024M
        reservations:
          cpus: '0.1'
          memory: 128M      
    labels:
      kompose.service.expose: app

networks:
  default:
    name: ns-app